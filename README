Team Member:
Guodong Hu(gh84)
Guotao Dong(gd50)

Build program by ./bulid
Run program as sudo ./proxy <port>

Configure Firefox by following steps:
1. Preference
2. Advance
3. Network
4. Connection Settings
5. Manual proxy Configuration
6. Type in the proxy IP Address and port

Note:
This program has not implemented the HTTP caching rules and all it does for caching is a simple caching for HTTP GET
Key feature:
The program is run as a daemon and it has its own log under /var/log/erss-proxy.log
Suppport HTTP GET, POST and HTTPS 
Cache for HTTP GET request
